---
- name: Setup Docker Environment
  hosts: all
  become: yes
  tasks:
    - name: Install Docker
      apt:
        name: [docker.io, docker-compose]
        state: present
        update_cache: yes
    - name: Ensure Docker is running
      service:
        name: docker
        state: started